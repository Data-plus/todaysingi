[build]
  command = "python scripts/export_products.py && npm --prefix admin ci && npm --prefix admin run build:netlify"
  publish = "site"

[[redirects]]
  from = "/admin/*"
  to = "/admin/index.html"
  status = 200
